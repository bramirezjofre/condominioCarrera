<% if (typeof isDemoBanner === 'undefined' || isDemoBanner) { %>
  <div class="bg-gold-100 text-gold-700 border-b border-gold-300 text-xs text-center py-1">
    Modo demostracion. Los datos se restablecen al reiniciar el servidor.
  </div>
<% } %>