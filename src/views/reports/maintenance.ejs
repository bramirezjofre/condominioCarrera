<div class="space-y-6">
  <%- include('../partials/page-header', { titleText: title, subtitleText: 'Mantenimientos por estado y prioridad', actions: [{ label: 'Exportar', href: '#', class: 'btn-secondary' }] }) %>

  <div class="grid gap-4 md:grid-cols-2 lg:grid-cols-4">
    <%- include('../partials/stat-card', { label: 'Nuevos', value: 3 }) %>
    <%- include('../partials/stat-card', { label: 'En curso', value: 5 }) %>
    <%- include('../partials/stat-card', { label: 'En revision', value: 2 }) %>
    <%- include('../partials/stat-card', { label: 'Completados', value: 12 }) %>
  </div>
</div>