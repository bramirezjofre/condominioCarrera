<!DOCTYPE html>
<html lang="es">
<head>
  <meta charset="UTF-8" />
  <title><%= title %> | Condominio JMC</title>
  <link rel="stylesheet" href="/css/app.css" />
</head>
<body class="bg-app text-app min-h-screen flex items-center justify-center px-4">
  <div class="card max-w-md text-center">
    <h1 class="text-2xl font-bold text-navy-900 mb-2"><%= message %></h1>
    <% if (requestId) { %>
      <p class="text-xs text-muted">ID de solicitud: <%= requestId %></p>
    <% } %>
    <a href="/app" class="inline-block mt-4 bg-navy-900 hover:bg-navy-800 text-white px-4 py-2 rounded">Volver</a>
  </div>
</body>
</html>